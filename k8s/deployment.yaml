# Minimal Kubernetes Deployment for a edgecommons component. With --platform auto the library detects
# KUBERNETES from the ServiceAccount token; config source defaults to CONFIGMAP, transport to MQTT
# (broker config from the mounted ConfigMap), identity from the Downward API — so NO args are needed.
# Build the image from ./Dockerfile and push to your registry (e.g. ghcr.io/<owner>/ethernet-ip-adapter)
# or `kind load` it for a local cluster, then set `image:` below.
#
# NOTE: object names are DNS-1123 (lower-kebab) — the Greengrass component name is PascalCase, but a
# Kubernetes metadata.name / ConfigMap name cannot be, so this pack uses `ethernet-ip-adapter`.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ethernet-ip-adapter
  labels:
    app.kubernetes.io/name: ethernet-ip-adapter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ethernet-ip-adapter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ethernet-ip-adapter
    spec:
      automountServiceAccountToken: true   # the projected SA token is the platform=KUBERNETES auto-detect signal
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
      containers:
        - name: component
          image: REPLACE_ME   # e.g. ghcr.io/<owner>/ethernet-ip-adapter:latest (build ./Dockerfile)
          imagePullPolicy: IfNotPresent
          env:
            - name: POD_NAME
              valueFrom: { fieldRef: { fieldPath: metadata.name } }
            - name: POD_NAMESPACE
              valueFrom: { fieldRef: { fieldPath: metadata.namespace } }
            - name: NODE_NAME
              valueFrom: { fieldRef: { fieldPath: spec.nodeName } }
          # Only the health and metrics listeners have a fixed port. Class-1 implicit I/O (mode:
          # push) declares none: the adapter binds an ephemeral UDP source port per push session and
          # advertises it to the device in the ForwardOpen, and 2222 is the device's destination
          # port, not a port the adapter listens on. A device inside the cluster reaches the pod IP
          # directly; a PLC outside it needs the pod IP routable from the device network
          # (hostNetwork: true, or a routable pod CIDR) — no NAT on the return path.
          ports:
            - { name: health, containerPort: 8081 }
            - { name: metrics, containerPort: 9090 }
          startupProbe:
            httpGet: { path: /startupz, port: health }
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /livez, port: health }
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /readyz, port: health }
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: config, mountPath: /etc/edgecommons, readOnly: true }   # WHOLE volume, never subPath (hot-reload)
            - { name: tmp, mountPath: /tmp }
      volumes:
        - name: config
          configMap: { name: ethernet-ip-adapter-config }
        - name: tmp
          emptyDir: {}
